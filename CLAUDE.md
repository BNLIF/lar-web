## Agent skills

### Backlog

Issues live in GitHub Issues on `BNLIF/lar-web`. See `docs/agents/backlog.md`.

### Triage labels

Default label vocabulary (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context — each top-level folder is a separate site root; `CONTEXT-MAP.md` points to per-folder `CONTEXT.md` files. See `docs/agents/domain.md`.
